/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTipeFormatPipePipe } from './DateTipeFormatPipe.pipe';

describe('Pipe: DateTipeFormatPipee', () => {
  it('create an instance', () => {
    let pipe = new DateTipeFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
